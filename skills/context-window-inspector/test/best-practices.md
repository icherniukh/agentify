# Best Practices

## File Operations
- Use `Read` tool before editing files
- Use `Glob` to find files by pattern
- Use `Grep` to search content

## Agent Delegation
- Delegate to appropriate subagents
- Use `run_in_background=true` for exploration
- Always provide session_id for continuity